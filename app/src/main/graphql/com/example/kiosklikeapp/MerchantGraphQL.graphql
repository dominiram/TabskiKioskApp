query GetMerchantBranding($id: ID!) {
    merchant(id: $id) {
        id
        branding {
            logoUrl
            coverUrl
            buttonBgColor
            buttonTextColor
            coverBrightness
        }
    }
}
